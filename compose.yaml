services:
  mongodb:
    image: 'mongo:latest'
    environment:
      MONGO_INITDB_DATABASE: mongo_db
      MONGO_INITDB_ROOT_PASSWORD: my_password
      MONGO_INITDB_ROOT_USERNAME: alex
    ports:
      - 27017:27017
  redis:
    image: redis:latest
    ports:
      - 6379:6379
